id: iserv
name: IServ School Assistant
i18n:
  default_locale: de
  supported_locales:
    - de
routing: inline
session:
  trigger: mention
  idle_timeout_minutes: 30
memory:
  policy: none
  top_k: 0
